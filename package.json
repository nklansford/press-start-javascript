{
  "name": "javascript-exercises",
  "version": "0.0.0",
  "description": "A personal development site for vanilla JavaScript",
  "homepage": "",
  "main": "index.html",
  "scripts": {
    "sync": "browser-sync start -s \"src\" -f \"src/**/*.*\"",
    "start": "npm-run-all -p sync",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:html": "cp src/index.html build",
    "build:assets": "cp -r src/assets build",
    "build": "npm-run-all build:assets build:html"
  },
  "author": "Noelle Lansford",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.27.4",
    "npm-run-all": "^4.1.5"
  }
}
